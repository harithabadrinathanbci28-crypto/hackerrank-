def count_substring(string, sub_string):
    rep = 0
    
    f = string.find(sub_string)
    while (f != -1):
        rep = rep + 1
        string = string[f+1:]
        f = string.find(sub_string)

    return rep
