if __name__ == "__main__":
    t = int(input())
    
    for _ in range(t):
        s = input().strip()
    
        try:
            _ = float(s)
            if '.' in s:
                print(True)
            else:
                print(False)
        except ValueError:
            print(False)
