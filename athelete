def sort_arr_by_kth_element(arr: list[list], k: int) -> list[list]:
    n = len(arr)
    for i in range(1, n):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j][k] > key[k]:
            arr[j + 1] = arr[j]
            j = j - 1
        arr[j + 1] = key
    return arr
